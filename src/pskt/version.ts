/**
 * PSKT Version enum representing the version of the Partially Signed Kaspa Transaction
 */
export enum Version {
  Zero = 0
}

/**
 * Default version constant
 */
export const DEFAULT_VERSION = Version.Zero;
